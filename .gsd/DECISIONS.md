# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D047 | M004 planning | M004 | Agent process spawning mechanism for build execution | Use node:child_process.spawn for direct CLI execution, not tmux control mode | ARCHITECTURE.md describes a tmux bridge but the current codebase uses execFile for git/gh operations and has no tmux integration. Introducing tmux control mode parsing, session management, and binary streaming is a large subsystem. Direct spawn gives process lifecycle (spawn, stdout/stderr streaming, exit code) with zero new infrastructure. Tmux can be revisited in M006 if terminal immersion becomes a priority; M004's goal is supervision, not terminal forwarding. | Yes — revisit in M006 if terminal forwarding becomes a priority |
| D001 | M001 | scope | Primary operator model | Single owner-operator first | The user wants Lumon optimized for one person managing many projects, not a shared team product yet. | Yes — if multi-operator demand becomes primary |
| D002 | M001 | pattern | Pre-build progression | Explicit stage approvals between major phases | The user wants deliberate approval gates before the project advances toward build. | No |
| D003 | M001 | arch | Build handoff threshold | Full pre-build package required before GSD starts | Viability, business plan, naming, domain/trademark signals, architecture, specs, and prototype all matter before autonomous build begins. | Yes — if the user later defines an intentionally leaner mode |
| D004 | M001 | arch | Workflow orchestrator | n8n is the first-class orchestration layer for discovery and approval | The user explicitly wants n8n to run the staged pre-build workflow and surface it through Lumon. | No |
| D005 | M001 | convention | Irreversible external actions | Prepare and recommend, then require explicit confirmation | Repo creation, domain purchase, and similar side effects must never happen silently. | No |
| D006 | M001 | pattern | Build supervision posture | Observe by default, with one bounded retry before escalation | The user wants Lumon to self-heal once where sensible but stop clearly when human judgment is needed. | Yes — if runtime behavior proves too passive or too noisy |
| D007 | M001 | convention | Dashboard priority | Stage state and agent state outrank economics and terminal immersion | The main screen should answer where every project is and what every agent is doing now. | No |
| D008 | M001 | scope | Project target type | Software-product ventures first | The current vision is specifically about building software ventures, not general business or media operations. | Yes — if later milestones intentionally broaden scope |
| D009 | 2026-03-13 | M001/S01 | structure | Control-shell state spine | Introduce a `src/lumon/*` reducer/context/selector spine and keep React Flow interaction state local to extracted surfaces. | S01 must retire cross-surface mock drift without turning canvas-only UI state into global state. |
| D010 | 2026-03-13 | M001/S01 | verification | Shared-state proof harness | Add Vitest + React Testing Library contract/integration tests as the required proof for the control-shell refactor. | The repo has no test framework and S01 needs repeatable proof that multiple surfaces consume the same canonical state. |
| D011 | 2026-03-13 | M001/S01 | data-model | Canonical Lumon state shape | Keep agents owned by projects, let execution stages reference agent IDs, and synthesize generic pipeline stages when the demo only provides partial orchestration detail. | This gives dashboard, floor, and orchestration selectors one source of truth now without blocking later stage-taxonomy or persistence work. |
| D012 | 2026-03-13 | M001/S01 | state-boundary | Surface-local interaction state | Keep modal intake drafts, terminal playback, and React Flow canvas state inside extracted surface modules while the Lumon provider owns only canonical project/agent/stage state and shared mutations. | T02 needs shared truth for cross-surface selection/status without turning presentational or speculative UI state into part of the domain contract. |
| D013 | 2026-03-13 | M001/S01 | selector-contract | Canonical floor projection | Project the Severance floor through a dedicated `selectFloorViewModel` selector plus a seeded `lumonFloorLayoutSeed`, and keep the floor component as a presentational/interaction shell over that contract. | T03 needed dashboard/floor synchronization and reproducible layout behavior without keeping ad hoc grouping, selection summaries, or render-time randomness inside the floor component. |
| D014 | 2026-03-13 | M001/S02 | arch | Project registry persistence boundary | Persist the canonical Lumon registry through a versioned localStorage envelope at the provider boundary, let explicit `initialState` override persisted state, and keep canonical project order append-only. | S02 needs durable reload continuity without a second registry path, test nondeterminism, or floor-anchor churn. |
| D015 | 2026-03-13 | M001/S02 | pattern | Canonical project spawn shape | Precompute each spawned project ID in the shell before dispatch so seeded agents and execution stages share the same stable prefix as the persisted project record. | T02 needs immediately usable orchestration/detail data on first render and stable child IDs even when duplicate project names force slug suffixes. |
| D016 | 2026-03-13 | M001/S03 | data-model | Pipeline and approval source of truth | Canonicalize the intake-to-handoff stage taxonomy and approval gate semantics in `src/lumon/*`, with dashboard and orchestration surfaces reading selector-derived pipeline summaries while React Flow stays presentation-only. | S03 needs to retire stage drift, satisfy the dashboard-first requirement, and leave stable stage/gate IDs for later n8n wait/resume integration. |
| D017 | 2026-03-13 | M001/S03 | compatibility | Canonical stage ID migration | Use stable `<projectId>:<stageKey>` stage IDs plus canonical gate IDs, and reconcile legacy stage selections through per-stage alias lists during rehydration instead of carrying a second registry format. | T01 needs one persisted contract for seeded, spawned, and previously saved projects without breaking reload continuity for older stage IDs. |
| D018 | 2026-03-13 | M001/S03 | selector-contract | Stage-first dashboard/orchestration projection | Build one selector-owned project pipeline view model with stable stage/gate/approval test surfaces, and have both dashboard and orchestration consume it while React Flow stays presentation-only. | T02 needs dashboard and orchestration to agree on waiting/blocked/handoff-ready truth and expose durable inspection points that survive reloads and UI layout changes. |
| D019 | 2026-03-14 | M001/S04 | ux-boundary | Project detail navigation seam | Keep dossier and handoff views as local `Overview` / `Dossier` / `Handoff` subviews inside the dashboard selected-project pane rather than adding a new shell-level tab or route. | S04 needs deeper project inspection without duplicating selection context or creating another navigation surface that can drift from the dashboard. |
| D020 | 2026-03-14 | M001/S04 | data-contract | M001 dossier and packet fidelity | Define the M001 dossier and handoff packet as stable section IDs and readiness states derived from current project, stage, and approval truth, and keep the brief intentionally thin until later milestones add real artifact records. | S04 must establish an inspectable contract now without persisting decorative placeholder content or implying artifact history that does not exist. |
| D021 | 2026-03-14 | M001/S05 | selector-contract | Pipeline-owned floor presence | Build Severed Floor departments and selected-project diagnostics from the canonical project pipeline view model, and map pipeline states to floor presentation at the project layer while leaving agent status unchanged. | S05 must retire dashboard/floor drift without creating a second approval/status system or faking project approval state through agents. |
| D022 | 2026-03-15 | M002 | arch | Thin bridge server for n8n integration | Introduce a lightweight Express server at `server/` that Vite proxies to during dev. This server manages n8n communication, artifact persistence, and approval resume state. The frontend cannot hold n8n credentials, manage resume URLs, or persist artifact payloads that exceed localStorage. | M001 is entirely client-side; M002 requires server-side state for n8n webhooks, artifact storage, and credential management. The server exists to bridge — not to become a full REST CRUD backend. |
| D023 | 2026-03-15 | M002 | data-model | Structured artifact references replace string stage output | Migrate `stage.output` from a plain string to `{ artifactId, summary, type }` with backward-compatible coercion in `createPipelineStage`. Existing string outputs become legacy format coerced on read. Artifacts live server-side, referenced by ID client-side. | Stage outputs from research, business planning, and architecture can be 50-100KB each. Inline localStorage storage will exceed browser limits. All 5 surfaces read `stage.output` through the selector chain so the migration must be compatible. |
| D024 | 2026-03-15 | M002 | arch | n8n Wait node resumeUrl as the approval primitive | Use n8n Wait node `$execution.resumeUrl` as the atomic approval mechanism. Lumon stores the resume URL server-side and calls it only when the operator explicitly approves. Never auto-resume. | The Wait node is purpose-built for human-in-the-loop approval gates — exactly the pattern Lumon needs. Rolling custom orchestration pause/resume would duplicate n8n's core value. |
| D025 | 2026-03-15 | M002 | pattern | Slice ordering: sync loop first, content stages additive | Prove the n8n↔Lumon sync contract with one real stage (intake/viability) before adding research, naming, and architecture stages. If the webhook→Wait→resume loop doesn't work, nothing downstream matters. | The server and sync loop are the foundational risk; content stages follow the same sub-workflow pattern and are additive once the primitive is proven. |
| D026 | 2026-03-15 | M002 | convention | Advisory labeling for domain and trademark signals | Domain availability and trademark results are clearly labeled as point-in-time advisory signals, not legal clearance. UI must include explicit disclaimers. | R009 and the context doc explicitly flag that these are advisory. Overstating certainty would be misleading. |
| D027 | 2026-03-15 | M002/S01 | data-model | Disk-based JSON artifact storage for bridge server | Use flat JSON files under `server/data/` instead of SQLite for artifact persistence. One file per artifact, indexed by ID. | S01 needs persistent artifacts but the volume is low (single-operator, tens of artifacts), SQLite adds a native dependency, and JSON files are debuggable by inspection. Upgrade to SQLite in a later milestone if volume or query patterns demand it. |
| D028 | 2026-03-15 | M002/S01 | testing | Vitest workspace for dual test environments | Use `vitest.workspace.js` to run server tests in Node environment while client tests run in jsdom, both sharing the same `npx vitest run` command. | Server tests need Node APIs (fs, http) while client tests need jsdom. A workspace config is cleaner than per-file environment pragmas and keeps a single test command. |
| D029 | 2026-03-15 | M002/S01 | arch | SSE per-projectId connection registry | Store active SSE Response objects in `Map<string, Set<Response>>` keyed by projectId, with inline `emitSSE` calls in trigger/callback/approve handlers. No event bus or external pub/sub. | At single-operator scale, inline emission is simpler and more auditable than an event bus. The registry cleans up on client disconnect. |
| D030 | 2026-03-15 | M002/S01 | pattern | Server sync exposed through LumonProvider actions context | `useServerSync` hook lives inside LumonProvider; `triggerPipeline` and `approvePipeline` are exposed as actions alongside existing dispatch-based actions rather than through a separate context. | Consumers already use `useLumonActions()` — adding trigger/approve there avoids a second hook import and keeps the action surface unified. Connection status uses a separate `ServerSyncContext` since it's read-only diagnostic state. |
| D031 | 2026-03-15 | M002/S01 | arch | Execution created before n8n webhook call | Bridge trigger handler creates the pipeline execution record before calling the n8n webhook, so the bridge's `executionId` is passed to n8n and echoed back in the callback. | n8n needs to know which bridge execution it's working for so the callback can be matched. Creating after the webhook call meant n8n never got the ID. |
| D032 | 2026-03-15 | M002/S01 | pattern | n8n workflow templates in n8n/workflows/ | Importable n8n workflow JSON files stored in `n8n/workflows/` directory with a README documenting setup. Callback URLs hardcoded to `host.docker.internal:3001` for Docker-based n8n. | n8n blocks `$env` access by default, so environment-variable-based URL configuration doesn't work without n8n admin config changes. Hardcoded URLs with README documentation is more reliable for the operator setup flow. |
| D033 | 2026-03-15 | M002/S02 | arch | Webhook registry with per-stage env var lookup and global fallback | `getWebhookUrl(stageKey)` checks `N8N_WEBHOOK_URL_<STAGE>` first, falls back to `N8N_WEBHOOK_URL`. The registry is config-only — no database, no admin UI. | S01 hardcoded a single `N8N_WEBHOOK_URL`. Multiple stages need different n8n webhook endpoints, but the existing convention must not break for setups that only configure the global URL. |
| D034 | 2026-03-15 | M002/S02 | arch | Per-stage execution tracking in pipeline.js | Change `projectIndex` from `Map<string, string>` to `Map<string, Map<string, string>>` so each stage has its own execution record per project. | Sequential sub-workflows mean multiple concurrent executions per project. The single-execution-per-project model from S01 can't track which stage is running without losing the other's state. |
| D035 | 2026-03-15 | M002/S02 | pattern | Sequential sub-workflow orchestration — no parallel triggers | Research stage fires business plan first, waits for its callback, then fires tech stack. Total latency is additive but predictable. | Parallel triggering introduces concurrent execution state complexity deferred to S04. Sequential is simpler, auditable, and acceptable for single-operator use. |
| D036 | 2026-03-15 | M002/S02 | pattern | Auto-trigger research after intake approval | The approve handler checks for a research webhook and auto-triggers the research pipeline when intake is approved. No operator action needed because research gate has `required: false`. | The research stage has no approval gate — making the operator manually trigger it would be pointless friction. Auto-trigger follows the gate semantics already defined in the model. |
| D037 | 2026-03-15 | M002/S02 | data-model | Dedicated append-artifact reducer action for multi-artifact accumulation | New `lumon/append-artifact` action reads existing `output.artifactIds`, appends, deduplicates, and sets latest as primary `artifactId`. Separate from `lumon/update-stage` to keep accumulation logic in one reducer case. | Using `update-stage` with merge semantics would require callers to pre-merge the artifactIds array. A dedicated action makes accumulation atomic and testable. The sync handler just dispatches the new artifact; the reducer handles the merge. |
| D038 | 2026-03-15 | M002/S02 | pattern | Module-level Map cache for useArtifact hook | Artifact content cached in a module-level `Map` rather than React context or reducer state. `clearArtifactCache()` exported for tests. | Artifact content is reference data, not application state — it shouldn't be serialized into localStorage, trigger re-renders through the provider, or bloat the state tree. Module-level cache provides cross-component deduplication with zero overhead. |
| D039 | M002/S03/T01 | architecture | Compound webhook STAGE_ENV_MAP key naming convention | Use full sub-stage names in compound keys (plan_naming_candidates, plan_domain_signals, plan_trademark_signals) matching the getWebhookUrl construction of stageKey + "_" + subStage | getWebhookUrl constructs compound keys as `${stageKey}_${subStage}` — e.g. "plan" + "_" + "naming_candidates" = "plan_naming_candidates". Short aliases like "plan_naming" would never match. Using full sub-stage names ensures the map lookup works without special-case aliasing. | Yes |
| D040 | M002/S03 | M002/S03/T03 | Interactive artifact renderer callback mechanism | Generic onAction({ type, ...payload }) prop-drilled through DossierStageOutputCard → ArtifactDetailPanel → ArtifactRenderer → sub-renderer chain, rather than a named callback or context provider | Prop-drilling keeps renderers testable without provider wrapping. The generic onAction pattern lets future interactive renderers follow the same interface — only NamingCandidatesRenderer uses it now, but the pattern is extensible without changing the component chain. | Yes — could move to context if the prop chain grows deeper than 3 levels |
| D041 | M003 planning | M003 | GSD bootstrap file format reference | Use this repo's own .gsd/ structure (GSD v2.28.0) as the reference implementation for generated project files | No formal GSD file format spec exists. This repo's .gsd/ directory is a working, proven instance of the format. Generating files that match the observed structure ensures GSD compatibility without overcoupling to undocumented internals. | Yes |
| D042 | M003 planning | M003 | Provisioning lifecycle boundary | Post-pipeline action triggered by explicit operator initiation after handoff approval, not a new pipeline stage | Adding a provisioning stage would break the existing 6-stage taxonomy (D016), every selector that counts stages, and the pipeline model that M001 and M002 proved. Provisioning is a one-time action on an approved project, not a repeatable stage with approval gates. | Yes |
| D043 | M003 planning | M003 | GitHub and git integration approach | Shell out to gh CLI and git via node:child_process execFile, not Octokit or isomorphic-git | gh is already installed and authenticated. Shelling out for a single operation (repo create + clone) is simpler and more reliable than importing a library. The research confirms this matches the established Node tooling pattern for git operations. | Yes |
| D044 | M003 planning | M003 | Workspace root configuration | Configurable via LUMON_WORKSPACE_ROOT env var, defaulting to parent of the Lumon repo directory | R014 requires separate workspaces per project. The workspace directory must be outside the Lumon repo. Hardcoding a path would break portability. The current directory structure (RoadToMillion/project/) provides a natural default. | Yes |
| D045 | M003 planning | M003 | GSD bootstrap file format reference | Use this repo's own .gsd/ structure (GSD v2.28.0) as the reference implementation for generated project files | No formal GSD file format spec exists. This repo's .gsd/ directory is a working, proven instance of the format. Generating files that match the observed structure ensures GSD compatibility without overcoupling to undocumented internals. | Yes |
| D046 | M004 planning | M004 | Build execution state model relationship to pre-build pipeline | Add buildExecution field to project model alongside existing execution engine, with its own state tracker in server/execution.js separate from pipeline.js | The pre-build pipeline (intake→handoff) orchestrates through n8n webhooks. Build execution is post-provisioning and uses a fundamentally different lifecycle (process spawning vs. webhook-driven). Overloading the pipeline trigger/callback/approve flow with build lifecycle events would conflate two distinct state machines. Separate service, shared SSE transport. | Yes |
| D048 | M004/S01 | ux-boundary | Build execution panel placement within dashboard | Render BuildExecutionPanel inside HandoffPanel after ProvisioningSection, self-gated on provisioningComplete | Follows the provisioning→build workflow order and keeps the operator in the handoff context. An extracted component (not inlined) keeps DashboardTab manageable per D046. The panel self-gates rather than requiring DashboardTab to manage visibility logic. | Yes |
| D049 | M004/S01 | arch | Agent output display strategy in BuildExecutionPanel | On-demand fetch from /api/execution/agent/:agentId/output ring buffer endpoint on card expand, not SSE streaming | SSE already carries throttled last-output lines for the activity card summary. Full output is only needed when the operator explicitly expands an agent card. On-demand fetch avoids SSE complexity for a rarely-used detail view while the ring buffer (1000 lines server-side) preserves all output regardless of display timing. | Yes — could add SSE streaming if operators need live tail-like behavior |
| D050 | M004/S02/T01, T05 | M004/S02 | Escalation state scope within the build execution model | retryCount tracked per-agent, escalation state tracked per-build (project level), escalated builds unconditionally place all agents in break-room on the floor | Retry is an agent-level behavior (the specific agent is re-spawned). Escalation is a project-level condition — the entire build pauses and requires operator attention. The floor reflects this by overriding individual agent locations when the build is escalated, giving the operator a clear visual signal that the project needs attention regardless of what individual agents are doing. | Yes — if multi-agent builds need per-agent escalation in the future |
| D051 | M005 planning | M005 | Confirmation boundary enforcement location | Server-enforced confirmation gate in server/external-actions.js — the execution endpoint checks for a prior confirmation record before calling the provider API. UI confirmation is supplementary, not authoritative. | R018 and D005 require explicit operator confirmation before irreversible external actions. R029 is an anti-feature explicitly banning side effects without confirmation. A UI-only guard can be bypassed by direct API calls. The server must be the enforcement boundary — it rejects execution requests that lack a matching confirmation record in the state tracker. This matches the principle that the server owns the truth for external state while the UI reflects and controls it. | No |
| D052 | M005/S01 | M005/S01 | Behavior of n8n domain-signals workflow when Porkbun API credentials are missing | Graceful degradation to simulated data with distinct engine metadata (`n8n-plan-domain-v1-simulated` vs `porkbun-live-v1`), rather than failing or blocking the pipeline | Failing when credentials are missing would block the entire plan stage during development, testing, or when the operator hasn't configured Porkbun yet. Returning simulated data with a distinct `metadata.engine` value lets the pipeline complete normally while making the data origin inspectable. S02 can check `metadata.engine` before allowing purchase actions on domains that were only checked with simulated data. | Yes |
| D053 | M005/S02 planning | M005/S02 | Porkbun domain purchase API endpoint | POST https://api.porkbun.com/api/json/v3/domain/create/{domain} with body { secretapikey, apikey, cost (pennies), agreeToTerms: "yes" }. Response: { status, domain, cost, orderId, balance, limits }. | Verified from official Porkbun API V3 documentation (porkbun.com/api/json/v3/documentation). The S02 research assumed the endpoint was /domain/register/{domain} but the actual endpoint is /domain/create/{domain}. The cost parameter is in pennies (integer), and agreeToTerms must be "yes" or "1" to acknowledge the domain registration agreement. The API enforces prerequisites: account must have registered at least one domain, verified email and phone, and sufficient account credit. Rate limited: 1 create attempt per 10 seconds, 10 successful creates per 24 hours. | No |
| D054 | M005/S02 planning | M005/S02 | Porkbun domain purchase API endpoint | POST https://api.porkbun.com/api/json/v3/domain/create/{domain} with body { secretapikey, apikey, cost (pennies), agreeToTerms: "yes" }. Response: { status, domain, cost, orderId, balance, limits }. | Verified from official Porkbun API V3 documentation. The S02 research assumed /domain/register/{domain} but the actual endpoint is /domain/create/{domain}. Cost in pennies (integer), agreeToTerms must be "yes". Rate limited: 1 create attempt per 10s, 10 successful creates per 24 hours. | No |
